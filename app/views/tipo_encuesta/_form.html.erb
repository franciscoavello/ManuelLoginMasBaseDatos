<%= form_for(@tipo_encuestum) do |f| %>
  <% if @tipo_encuestum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_encuestum.errors.count, "error") %> prohibited this tipo_encuestum from being saved:</h2>

      <ul>
      <% @tipo_encuestum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Tipo_id %><br>
    <%= f.number_field :Tipo_id %>
  </div>
  <div class="field">
    <%= f.label :Tipo_nombre %><br>
    <%= f.text_field :Tipo_nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
