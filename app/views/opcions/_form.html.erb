<%= form_for(@opcion) do |f| %>
  <% if @opcion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opcion.errors.count, "error") %> prohibited this opcion from being saved:</h2>

      <ul>
      <% @opcion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Opcion_id %><br>
    <%= f.number_field :Opcion_id %>
  </div>
  <div class="field">
    <%= f.label :Opcion_valor %><br>
    <%= f.number_field :Opcion_valor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
